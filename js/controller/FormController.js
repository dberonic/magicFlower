export class FormController {

    /**
     * Form controle 
     * Does nothin, could be extenden in the future
     * @param {FormView} view 
     * @param {FlowerBouquet} model 
     */
    constructor(view, model) {

        this.view = view;
        this.model = model;
    }
}